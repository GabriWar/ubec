# Server Configuration
PORT=3001
NODE_ENV=development

# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mtzview
DB_USER=mtzview
DB_PASSWORD=mtzview_secure_password

# CORS Configuration
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:5173,http://localhost:3001

# Frontend Path (for production)
FRONTEND_PATH=../dist
